<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ entry.title ?? 'Page' }}{% endblock %} | Personal Website</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background: #f8f9fa; }
        .container { max-width: 1200px; margin: 0 auto; padding: 2rem; background: white; min-height: 100vh; }
        header { background: #2c3e50; color: white; padding: 1rem 0; margin-bottom: 2rem; }
        nav { display: flex; gap: 2rem; max-width: 1200px; margin: 0 auto; padding: 0 2rem; }
        nav a { color: white; text-decoration: none; padding: 0.5rem 0; }
        nav a:hover { text-decoration: underline; }
        h1 { font-size: 2.5rem; margin-bottom: 1rem; color: #2c3e50; }
        h2 { font-size: 2rem; margin: 2rem 0 1rem; color: #34495e; }
        h3 { font-size: 1.5rem; margin: 1.5rem 0 0.75rem; color: #34495e; }
        .content { margin-bottom: 2rem; line-height: 1.8; }
        .meta { color: #666; font-size: 0.9rem; margin-bottom: 1rem; }
        .gallery { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 1rem; margin: 2rem 0; }
        .gallery img { width: 100%; height: 200px; object-fit: cover; border-radius: 8px; }
        .categories, .related { margin: 2rem 0; padding: 1rem; background: #f8f9fa; border-radius: 8px; }
        .tag { display: inline-block; padding: 0.25rem 0.75rem; margin: 0.25rem; background: #3498db; color: white; border-radius: 4px; text-decoration: none; font-size: 0.85rem; }
        .tag:hover { background: #2980b9; }
        .entry-list { list-style: none; }
        .entry-list li { margin-bottom: 2rem; padding-bottom: 2rem; border-bottom: 1px solid #eee; }
        .entry-list li:last-child { border-bottom: none; }
        .entry-list h3 { margin-top: 0; }
        .entry-list a { color: #2c3e50; text-decoration: none; }
        .entry-list a:hover { color: #3498db; }
        footer { margin-top: 4rem; padding-top: 2rem; border-top: 1px solid #ddd; color: #666; text-align: center; }
    </style>
</head>
<body>
    <header>
        <nav>
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/creative">Creative</a>
            <a href="/thoughts">Thoughts</a>
            <a href="/travels">Travels</a>
            <a href="/contact">Contact</a>
        </nav>
    </header>

    <div class="container">
        {% block content %}{% endblock %}

        <footer>
            <p>&copy; {{ now|date('Y') }} Personal Website. Built with Craft CMS.</p>
        </footer>
    </div>
</body>
</html>
